# Dominion

> Your AI development team, generated from your codebase.

A Claude Code plugin that analyzes your project and generates a complete AI development methodology: specialized agents, curated tools, governance rules, code conventions, git workflow, and a CLI toolkit — all committed to git.

New developers get best practices on `git clone`. The system improves itself after every phase.
