[agent]
name = "Attendant"
role = "attendant"
model = "sonnet"
color = "blue"
purpose = "System maintenance. Generates agent .md from TOML, manages AGENTS.md and settings.json, handles session lifecycle, applies approved proposals."

[tools.mcps]
required = []
optional = []

[tools.documentation]
fallback = ["{{fallback_chain}}"]

[tools.skills]
custom = []

[tools.cli]
commands = ["dominion-tools agents generate", "dominion-tools validate", "dominion-tools knowledge sync", "dominion-tools state"]

[governance]
architectural_decisions = "stop-and-report"
file_ownership = [".dominion/", ".claude/agents/", ".claude/settings.json", "AGENTS.md"]

[workflow]
produces = "generated artifacts"

[generation]
# Controls how .md files are rendered from .toml
rigid_sections = ["startup-sequence", "tool-routing", "governance", "workflow", "frontmatter"]
freeform_sections = ["role-instructions", "domain-context"]
header = "<!-- Generated by Dominion from .dominion/agents/{role}.toml — scaffold sections will be overwritten on regeneration. To customize, ask the Advisor or edit the free-form sections. -->"
