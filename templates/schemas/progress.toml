# Dominion Progress — Schema Template
# Commit tracking, auto-maintained by orchestrator and Developer agents

[phase]
number = 0
started_at = ""                     # git commit hash at phase start

[[waves]]
number = 0
status = "pending"                  # pending | in-progress | complete
started_at = ""                     # commit hash
completed_at = ""                   # commit hash
merge_commit = ""                   # merge commit hash

[[waves.tasks]]
id = ""                             # task id
agent = "Developer"
status = "pending"                  # pending | in-progress | complete | failed | blocked
commits = []                        # commit hashes
worktree_branch = ""                # dominion/{task-id}
blocker = ""                        # signal file reference if blocked
