# Validation Checks

## Check 1: TOML Integrity

Read every `.toml` file in `.dominion/` (recursive). Verify each parses without errors.

Pass: all files parse
Fail: list files that fail to parse with error details

## Check 2: Agent TOML-MD Consistency

For every `.dominion/agents/*.toml`:
- Verify a matching `.claude/agents/*.md` exists (same role name)
- Verify the .md file contains the Dominion header comment

Pass: all agents have matching .md files with headers
Fail: list missing or headerless .md files

## Check 3: AGENTS.md Roster

- Verify `AGENTS.md` exists at project root
- Verify it lists every agent from `.dominion/agents/`
- Verify it contains the "auto-generated by Dominion" header

Pass: AGENTS.md exists and lists all agents
Fail: missing or incomplete roster

## Check 4: settings.json Permissions

- Verify `.claude/settings.json` exists
- Verify it contains `Bash(dominion-tools *)` permission
- For each agent's required MCPs, verify the MCP's read permissions are listed

Pass: all required permissions present
Warn: optional MCP permissions missing (list which)
Fail: settings.json missing or missing required permissions

## Check 5: CLI Completeness

- Read `.dominion/specs/cli-spec.toml`
- For each command in `minimum_commands`, verify `dominion-tools {command} --help` works

Pass: all commands respond to --help
Fail: list commands that fail

## Check 6: Documentation Fallback Chain

- Verify `dominion.toml` has a `[documentation]` section
- Verify it has at least one source
- Verify it has a `[documentation.fallback]` with `action = "stop-and-ask"`

Pass: chain configured with stop-and-ask terminal
Fail: missing or incomplete chain

## Check 7: CLAUDE.md Exists

- Verify `CLAUDE.md` exists at project root
- Verify it's non-empty

Pass: exists and non-empty
Warn: missing (init may not have completed)

## Check 8: State Consistency

- Verify `.dominion/state.toml` exists and parses
- Verify `[init]` section has `completed_at` set

Pass: state file valid with init timestamp
Fail: missing or invalid state
